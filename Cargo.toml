[package]
name = "wasm-tracing-macro"
version = "0.1.1"
edition = "2024"
description = "OTel tracing macro for functions for use in wasmCloud"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = "1.0.45"
syn = { version = "2.0.117", features = ["full"] }
